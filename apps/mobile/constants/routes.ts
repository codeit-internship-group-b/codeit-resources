const dirName = "(route)";

export const ROUTES = {
  HOME: "/index",
  DASHBOARD: `/${dirName}/dashboard`,
  MEETINGS: `/${dirName}/meetings`,
  SEATS: `/${dirName}/seats`,
  SETTINGS: `/${dirName}/settings`,
  NOT_FOUND: "/+not-found",
};
